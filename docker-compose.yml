services:
  linux:
    platform: linux/arm64
    # Linux Container
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    expose:
      - 8080
    image: bradbarnhill/barcode-api